name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, vps]
    steps:
      - name: Run deploy script
        run: |
          chmod +x /var/www/iti-comite/deploy.sh
          /var/www/iti-comite/deploy.sh
